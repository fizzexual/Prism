# PostgreSQL connection used by the server
DATABASE_URL=postgres://prism:prism@localhost:5432/prism

# Port the Express API listens on
PORT=9998

# Base URL the client uses for API calls. Leave empty to use the Vite dev proxy.
VITE_API_BASE=
